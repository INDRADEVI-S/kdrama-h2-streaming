This project is a full-stack web application developed using Spring Boot that simulates a K-Drama streaming platform. It demonstrates the implementation of RESTful APIs, layered architecture (Controller–Service–Repository), database integration, and dynamic frontend rendering using Thymeleaf.

The platform allows users to explore a collection of popular K-Dramas, view thumbnails, filter by genre, and watch embedded YouTube trailers directly within the web interface
